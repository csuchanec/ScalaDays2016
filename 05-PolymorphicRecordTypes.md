# Polymorphic Record Types in a Lifted Embedding

* Record types - fixed number of elements with known type
   * Usually named (tupples have names just _1 or _2)

* Polymorphic record types
   * Tuples
   * Other product-like Types
   * HList Types

* Functional Dependecnies - dependencies between type parameters
   * done with implicits
   *

*https://github.com/szeiger/slick/tree/toy-slick-scaladays2016